name: 'communications'
type: profile
description: 'Communications lightning subprofile'
core: '8.x'
# Optional: Declare your installation profile as a distribution
# This will make the installer auto-select this installation profile.
# The distribution_name property is used in the installer and other places as
# a label for the software being installed.
distribution:
  name: Communications
# Required modules
themes:
  - nrel_bootstrap
dependencies:
    - syslog
    - features_ui
    - admin_toolbar
    - bootstrap_layouts
    - linkit
    - google_analytics
    - big_pipe
    # - memcache
    - field_group
    - field_formatter_class
    - git_deploy
    - comment
    - imagemagick
    - image_effects
    - jquery_colorpicker
base profile:
  name: lightning
version: '8.x-2.05'
